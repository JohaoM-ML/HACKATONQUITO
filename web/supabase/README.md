# Migraciones Supabase — VENTANA SECA

Proyecto: `ventana-seca` (ref `mfmseatpeepadupfhmtd`)

| Archivo | Contenido |
|---------|-----------|
| `001_schema_inicial.sql` | Tablas, vista `indices_sector`, RLS, trigger de perfil |

Aplicadas vía MCP `apply_migration`. Para reaplicar en otro proyecto, pégalos en SQL Editor en orden.
